==== Implement a feature to add payload via subkeys ====
# Until now, changes in payload are possible only accessing a "primary" key in the json object.
# But we need to access values passing through more keys

- require:

